<%= form_with(model: abdo) do |form| %>
  <% if abdo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(abdo.errors.count, "error") %> prohibited this abdo from being saved:</h2>

      <ul>
        <% abdo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.hidden_field :user_id %>
  </div>

  <div>
    <%= form.label :exercise_id, style: "display: block" %>
    <%= form.select :exercise_id, Exercise.abdo.collect{|h|[h.name, h.id]} %>
  </div>

  <div>
    <%= form.label :nb, style: "display: block" %>
    <%= form.number_field :nb %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
