<%= form_with(model: cardio) do |form| %>
  <% if cardio.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cardio.errors.count, "error") %> prohibited this cardio from being saved:</h2>

      <ul>
        <% cardio.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :exercise_id, style: "display: block" %>
    <%= form.select :exercise_id, Exercise.cardio.collect{|h|[h.name, h.id]} %>
  </div>

  <div>
    <%= form.label :duree, style: "display: block" %>
    <%= form.time_field :duree %>
  </div>

  <div>
    <%= form.hidden_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
